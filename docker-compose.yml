services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - cb-index-data:/app/data
      - ./index.html:/app/index.html:ro
      - ./release_notes.md:/app/release_notes.md:ro
    restart: unless-stopped

volumes:
  cb-index-data:
